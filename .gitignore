# Dependencias de Node (Si llegas a instalar paquetes en el futuro)
node_modules/

# Archivos de entorno y secretos (¡NUNCA subir contraseñas!)
.env
.env.local
.env.*.local

# Archivos del sistema operativo (macOS / Windows)
.DS_Store
Thumbs.db

# Archivos de log (registros de errores temporales)
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Configuraciones del editor (VS Code, etc.)
.vscode/
.idea/

# Carpetas de construcción/empaquetado
dist/
build/